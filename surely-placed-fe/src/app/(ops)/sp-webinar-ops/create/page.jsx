import CreateSection from '@/components/WebinarOps/sections/CreateSection';

export const metadata = {
  title: 'Webinar Ops · Create',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <CreateSection />;
}
